@page "/"

@if(ComponentType != null)
{
    <DynamicComponent Type="@ComponentType" /> 
}

@code {
    [CascadingParameter(Name = "ComponentType")]
    public Type? ComponentType { get; set; }

    protected override void OnParametersSet()
    {
        
    }
}
