@page "/user/{UserId}"

@using Microsoft.AspNetCore.Authorization

@attribute [Authorize(Policy = MagFlow.Shared.Constants.Policies.USER_ADD_OR_ADMIN)]

<h3>User</h3>

@code {
    [Parameter]
    public string UserId { get; set; }
}
